import React from 'react';
import { StyleSheet, View } from 'react-native';

const TopNav = () => <View style={styles.spacer} />;

const styles = StyleSheet.create({
  spacer: {
    height: 8
  }
});

export default TopNav;
